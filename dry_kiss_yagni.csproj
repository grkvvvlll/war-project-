<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<OutputType>Exe</OutputType>

		<TargetFramework>net8.0</TargetFramework>

		<EnableDefaultCompileItems>false</EnableDefaultCompileItems>

		<Nullable>enable</Nullable>
		<ImplicitUsings>enable</ImplicitUsings>
	</PropertyGroup>

	<ItemGroup>
		<!-- добавляем оба файла в проект -->
		<Compile Include="dry_kiss_yagni_new.cs" />
		<Compile Include="dry_kiss_yagni_old.cs" />
	</ItemGroup>

	<ItemGroup>
		<!-- но old убираем из сборки, он лежит только для сравнения -->
		<Compile Remove="dry_kiss_yagni_old.cs" />
	</ItemGroup>
</Project>
