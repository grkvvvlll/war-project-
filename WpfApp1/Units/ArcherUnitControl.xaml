<UserControl x:Class="WpfPresentation.Views.Units.ArcherUnitControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="48" Height="72">

    <UserControl.Resources>
        <Storyboard x:Key="AttackAnim">
            <DoubleAnimation Storyboard.TargetName="Arrow"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="0" To="20" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetName="Arrow"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="HitAnim">
            <ColorAnimation Storyboard.TargetName="Root"
                    Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                    To="#33E24B4A" Duration="0:0:0.1"
                    AutoReverse="True"/>
        </Storyboard>
        <Storyboard x:Key="DeathAnim">
            <DoubleAnimation Storyboard.TargetName="Root"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.5"/>
        </Storyboard>
    </UserControl.Resources>

    <Canvas x:Name="Root" Width="48" Height="72" Background="Transparent">
        <Canvas.RenderTransform>
            <TranslateTransform X="0" Y="0"/>
        </Canvas.RenderTransform>
        <Canvas.Resources>
            <SolidColorBrush x:Name="StrokeColor" x:Key="StrokeBrush" Color="#B4B2A9"/>
        </Canvas.Resources>

        <!-- Голова -->
        <Ellipse Canvas.Left="17" Canvas.Top="3" Width="14" Height="14"
                 Stroke="{StaticResource StrokeBrush}" StrokeThickness="1.5" Fill="Transparent"/>

        <!-- Тело -->
        <Line X1="24" Y1="17" X2="24" Y2="42" Stroke="{StaticResource StrokeBrush}" StrokeThickness="1.5"/>

        <!-- Лук -->
        <Path Stroke="{StaticResource StrokeBrush}" StrokeThickness="2" Fill="Transparent"
              Data="M10,18 Q4,28 10,38"/>
        <Line X1="10" Y1="18" X2="10" Y2="38" Stroke="{StaticResource StrokeBrush}" StrokeThickness="1"/>

        <!-- Тетива -->
        <Line X1="10" Y1="18" X2="10" Y2="38" Stroke="{StaticResource StrokeBrush}" StrokeThickness="0.8"/>

        <!-- Рука к луку -->
        <Line X1="24" Y1="24" X2="10" Y2="28" Stroke="{StaticResource StrokeBrush}" StrokeThickness="1.5"/>
        <!-- Рука натягивает -->
        <Line X1="24" Y1="24" X2="36" Y2="28" Stroke="{StaticResource StrokeBrush}" StrokeThickness="1.5"/>

        <!-- Стрела (анимируется) -->
        <Canvas x:Name="Arrow">
            <Canvas.RenderTransform>
                <TranslateTransform X="0" Y="0"/>
            </Canvas.RenderTransform>
            <Line X1="10" Y1="28" X2="36" Y2="28" Stroke="{StaticResource StrokeBrush}" StrokeThickness="1"/>
            <Polygon Points="36,28 32,26 32,30" Fill="{StaticResource StrokeBrush}"/>
        </Canvas>

        <!-- Ноги -->
        <Line X1="24" Y1="42" X2="16" Y2="58" Stroke="{StaticResource StrokeBrush}" StrokeThickness="1.5"/>
        <Line X1="24" Y1="42" X2="32" Y2="58" Stroke="{StaticResource StrokeBrush}" StrokeThickness="1.5"/>
    </Canvas>
</UserControl>